# Лабораторная работа 5: Алгоритмы отсечения отрезков и многоугольников

**Вариант 12**

## 📋 Описание

Данная программа реализует алгоритмы отсечения отрезков и многоугольников для компьютерной графики. Программа выполнена в соответствии с заданием лабораторной работы №5 для варианта 12.

## 🎯 Цель работы

Закрепление теоретического материала и практическое освоение основных методов и алгоритмов отсечения отрезков и многоугольников.

## ✨ Реализованный функционал

### Вариант 12 включает:

#### Часть 1: Алгоритм средней точки
- Отсечение отрезков прямоугольным окном
- Работает по принципу деления отрезка пополам
- Находит видимые части отрезков внутри прямоугольного окна

#### Часть 2: Алгоритм отсечения выпуклым многоугольником
- Реализован алгоритм Кируса-Бека
- Отсечение отрезков произвольным выпуклым многоугольником
- Использует параметрическое представление отрезка

## 🔧 Функциональные возможности программы

### 1. Визуализация системы координат
- Автоматический масштаб в зависимости от входных данных
- Координатная сетка для удобства восприятия
- Оси координат с делениями

### 2. Ввод данных
- **Отрезки**: Ввод координат отрезков в формате `X1 Y1 X2 Y2` (каждый отрезок на новой строке)
- **Прямоугольное окно**: Ввод координат в формате `Xmin Ymin Xmax Ymax`
- **Выпуклый многоугольник**: Ввод координат вершин в формате `X Y` (каждая вершина на новой строке)

### 3. Загрузка из файла
- Возможность загрузки данных из текстового файла
- Формат файла:
```
n                          (количество отрезков)
X1_1 Y1_1 X2_1 Y2_1       (координаты отрезка 1)
X1_2 Y1_2 X2_2 Y2_2       (координаты отрезка 2)
...
X1_n Y1_n X2_n Y2_n       (координаты отрезка n)
Xmin Ymin Xmax Ymax       (координаты прямоугольного окна)
X1 Y1                      (вершина 1 многоугольника)
X2 Y2                      (вершина 2 многоугольника)
...
```

### 4. Цветовая дифференциация
- **Красный цвет** - исходные отрезки (до отсечения)
- **Синий цвет** - прямоугольное окно отсечения
- **Зеленый цвет** - видимые части отрезков после отсечения прямоугольным окном
- **Фиолетовый цвет** - выпуклый многоугольник отсечения
- **Оранжевый цвет** - видимые части отрезков после отсечения многоугольником

### 5. Интерактивный интерфейс
- Удобная панель управления
- Раздельный ввод для отрезков, прямоугольного окна и многоугольника
- Кнопки "Визуализировать" и "Очистить"
- Легенда с пояснениями цветов

## 📦 Требования

- **Python 3.8 или выше**
- **tkinter** (входит в стандартную библиотеку Python)

### Проверка установки Python:
```bash
python --version
```
или
```bash
python3 --version
```

## 🚀 Установка и запуск

### Вариант 1: Локальный запуск (рекомендуется)

#### Шаг 1: Клонирование репозитория
```bash
git clone <URL вашего репозитория>
cd <название папки проекта>
```

#### Шаг 2: Установка зависимостей
```bash
pip install -r requirements.txt
```
*Примечание: Файл requirements.txt пуст, так как используются только стандартные библиотеки Python*

#### Шаг 3: Запуск программы
```bash
python main.py
```
или
```bash
python3 main.py
```

### Вариант 2: Скачивание только исполняемого файла

#### Для Windows:

1. Перейдите в раздел **Releases** на GitHub
2. Скачайте последнюю версию `main.exe`
3. Запустите двойным кликом или через командную строку:
```cmd
main.exe
```

#### Создание EXE файла самостоятельно:

Если вы хотите создать исполняемый файл самостоятельно:

1. Установите PyInstaller:
```bash
pip install pyinstaller
```

2. Создайте EXE:
```bash
pyinstaller --onefile --windowed --name="Лаб_раб_5_Вариант_12" main.py
```

3. Готовый файл будет в папке `dist/`

## 📖 Инструкция по использованию

### Пример работы с программой:

#### 1. Запуск программы
- Запустите файл `main.py`
- Откроется окно с интерфейсом программы

#### 2. Ввод данных вручную

**Пример входных данных:**

**Отрезки** (в левом верхнем поле):
```
0 0 300 300
50 300 300 50
150 0 150 300
0 150 300 150
100 100 200 250
250 100 50 200
```

**Прямоугольное окно**:
```
50 50 250 200
```

**Выпуклый многоугольник**:
```
300 300
500 300
500 500
300 500
```

#### 3. Загрузка из файла
- Нажмите кнопку "Загрузить данные из файла"
- Выберите текстовый файл с данными
- Данные автоматически загрузятся в соответствующие поля

**Пример файла `input.txt`:**
```
6
0 0 300 300
50 300 300 50
150 0 150 300
0 150 300 150
100 100 200 250
250 100 50 200
50 50 250 200
300 300
500 300
500 500
300 500
```

#### 4. Визуализация
- Нажмите кнопку "Визуализировать"
- На холсте отобразятся:
  - Исходные отрезки (красным)
  - Прямоугольное окно (синим)
  - Видимые части после отсечения прямоугольником (зеленым)
  - Выпуклый многоугольник (фиолетовым)
  - Видимые части после отсечения многоугольником (оранжевым)

#### 5. Очистка данных
- Нажмите кнопку "Очистить" для удаления всех данных и очистки холста

## 🔬 Подробное описание алгоритмов

### 1. Алгоритм средней точки (Midpoint Subdivision Algorithm)

#### Принцип работы:
1. Проверяет, находятся ли оба конца отрезка внутри окна (тривиальный случай принятия)
2. Проверяет, находятся ли оба конца с одной стороны снаружи окна (тривиальный случай отбрасывания)
3. Если отрезок частично видим:
   - Находит середину отрезка
   - Заменяет внешний конец серединой
   - Повторяет процесс до достижения требуемой точности

#### Преимущества:
- Простота реализации
- Надежность работы
- Подходит для всех типов отрезков

#### Параметры точности:
- `epsilon = 0.001` - точность позиционирования
- `max_iterations = 50` - максимальное число итераций

### 2. Алгоритм Кируса-Бека (Cyrus-Beck Algorithm)

#### Принцип работы:
1. Использует параметрическое представление отрезка: `P(t) = P1 + t(P2 - P1)`, где `0 ≤ t ≤ 1`
2. Для каждого ребра многоугольника:
   - Вычисляет внутреннюю нормаль
   - Определяет параметры входа и выхода отрезка
3. Находит максимальный параметр входа (`t_enter`) и минимальный параметр выхода (`t_exit`)
4. Если `t_enter ≤ t_exit`, отрезок видим на интервале `[t_enter, t_exit]`

#### Преимущества:
- Работает с выпуклыми многоугольниками произвольной формы
- Высокая эффективность
- Параметрический подход позволяет точно определить видимую часть

#### Математическая основа:
- Векторное произведение для определения нормали
- Скалярное произведение для вычисления параметров пересечения
- Логика обработки входящих/выходящих ребер

## 📊 Структура проекта

```
Лаб_раб_5/
│
├── main.py              # Основной файл программы
├── requirements.txt     # Зависимости проекта
├── .gitignore          # Игнорируемые файлы для Git
├── README.md           # Документация (этот файл)
│
└── examples/           # (опционально) Примеры входных файлов
    └── input.txt
```

## 💡 Примеры использования

### Пример 1: Простые отрезки и прямоугольное окно

**Входные данные:**
```
Отрезки:
0 0 100 100
50 0 50 100

Прямоугольное окно:
25 25 75 75
```

**Результат:**
- Отрезок `(0,0)-(100,100)` будет отсечен до `(25,25)-(75,75)`
- Отрезок `(50,0)-(50,100)` будет отсечен до `(50,25)-(50,75)`

### Пример 2: Отрезки и треугольное окно

**Входные данные:**
```
Отрезки:
0 0 200 200
0 200 200 0

Многоугольник (треугольник):
100 50
200 150
50 150
```

**Результат:**
- Оба отрезка будут отсечены треугольником
- Видимые части будут отображены оранжевым цветом

## 🐛 Решение проблем

### Проблема: "tkinter не найден"
**Решение:**
- **Ubuntu/Debian**: `sudo apt-get install python3-tk`
- **Fedora**: `sudo dnf install python3-tkinter`
- **macOS**: tkinter включен в стандартную установку Python
- **Windows**: tkinter включен в стандартную установку Python

### Проблема: Программа не запускается
**Решение:**
1. Убедитесь, что Python установлен: `python --version`
2. Попробуйте: `python3 main.py` вместо `python main.py`
3. Проверьте права доступа к файлу

### Проблема: Ошибка при загрузке файла
**Решение:**
1. Проверьте формат данных в файле
2. Убедитесь, что используется правильная кодировка (UTF-8)
3. Проверьте, что числа разделены пробелами

## 📝 Формат входного файла

```
6                          ← Количество отрезков (n)
0 0 300 300               ← Отрезок 1: X1 Y1 X2 Y2
50 300 300 50             ← Отрезок 2
150 0 150 300             ← Отрезок 3
0 150 300 150             ← Отрезок 4
100 100 200 250           ← Отрезок 5
250 100 50 200            ← Отрезок 6
50 50 250 200             ← Прямоугольное окно: Xmin Ymin Xmax Ymax
300 300                   ← Вершина 1 многоугольника: X Y
500 300                   ← Вершина 2
500 500                   ← Вершина 3
300 500                   ← Вершина 4
```
